/* Realized by : KIHAL Aya */
#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9).
 * @c: input value.
 *
 * Return: 1 if digit else 0 (False)
 *
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	return (0);
}


